# Copy to .env (gitignored) for local dev. Vite only exposes vars prefixed VITE_.
# See CLAUDE.md "Google SSO" for how to obtain this from Google Cloud Console.
# The Google Sign-In button is simply hidden if this is unset.
VITE_GOOGLE_CLIENT_ID=
