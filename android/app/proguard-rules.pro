# Nothing app-specific needs keeping — the app has no reflection-heavy code,
# it's a thin WebView shell. Default Android/AndroidX consumer rules cover
# the rest via the dependencies' own proguard-consumer files.
